version: '3.8'
services:
  adaptive_immunity_db:
    build: .
    container_name: vertice-adaptive-immunity-db
    hostname: vertice-adaptive-immunity-db
    ports:
    - 5432:5432
    environment:
    - SERVICE_NAME=adaptive_immunity_db_service
    - SERVICE_HOST=0.0.0.0
    - SERVICE_PORT=5432
    - LOG_LEVEL=INFO
    - ENVIRONMENT=development
    - REDIS_URL=redis://redis:6379
    - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/db
    - MONGO_URL=mongodb://mongo:27017/db
    networks:
    - maximus-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:5432/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on: {}
  adaptive_immunity_db-sidecar:
    image: vertice-registry-sidecar:latest
    container_name: vertice-adaptive-immunity-db-sidecar
    environment:
    - SERVICE_NAME=adaptive_immunity_db_service
    - SERVICE_HOST=vertice-adaptive-immunity-db
    - SERVICE_PORT=5432
    - SERVICE_HEALTH_ENDPOINT=/health
    - REGISTRY_URL=http://vertice-register-lb:80
    - HEARTBEAT_INTERVAL=30
    - INITIAL_WAIT_TIMEOUT=60
    depends_on:
      adaptive_immunity_db:
        condition: service_started
    networks:
    - maximus-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 64M
networks:
  maximus-network:
    external: true
