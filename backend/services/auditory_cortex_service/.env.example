# ═══════════════════════════════════════════════════════════════════════════
# AUDITORY CORTEX SERVICE - Environment Configuration
# Biomimetic Auditory Processing for Cybersecurity
# ═══════════════════════════════════════════════════════════════════════════

# Server Configuration
HOST=0.0.0.0
PORT=8007
LOG_LEVEL=INFO
PYTHONUNBUFFERED=1

# C2 Beacon Detection Configuration (Cochlear FFT)
C2_SUSPICIOUS_PERIOD_MIN=5.0
C2_SUSPICIOUS_PERIOD_MAX=3600.0
C2_MIN_SNR=5.0
C2_PROMINENCE_THRESHOLD=3.0

# FFT Analysis Parameters
FFT_MIN_FREQUENCY=0.001
FFT_MAX_FREQUENCY=1.0
FFT_WINDOW_FUNCTION=hann

# Binaural Correlation Configuration (ITD/ILD)
BINAURAL_MAX_TIME_WINDOW_SECONDS=300.0
BINAURAL_MIN_CORRELATION_CONFIDENCE=0.6
BINAURAL_MAX_ITD_SECONDS=60.0

# Correlation Thresholds
ITD_WEIGHT=0.5
ILD_WEIGHT=0.5

# Cocktail Party Triage Configuration (ASA)
ASA_TEMPORAL_WINDOW_SECONDS=300.0
ASA_MIN_CUE_STRENGTH=0.5

# ASA Cue Weights
ASA_PITCH_WEIGHT=0.3
ASA_TIMBRE_WEIGHT=0.3
ASA_TEMPORAL_WEIGHT=0.25
ASA_SPATIAL_WEIGHT=0.15

# Stream Coherence Thresholds
MIN_PITCH_CONSISTENCY=0.7
MIN_TIMBRE_CONSISTENCY=0.6
MIN_TEMPORAL_COHERENCE=0.5
MIN_SPATIAL_COHERENCE=0.4

# TTP Signature Recognition Configuration
TTP_MIN_SIMILARITY_THRESHOLD=0.6
TTP_MIN_INCIDENTS_FOR_CONFIDENCE=5

# Signature Feature Weights
TTP_PATTERN_WEIGHT=0.4
TTP_TIMING_WEIGHT=0.25
TTP_PITCH_WEIGHT=0.2
TTP_TIMBRE_WEIGHT=0.15

# Performance Configuration
ENABLE_PARALLEL_FFT=true
FFT_NUM_WORKERS=4
ENABLE_STREAM_CACHING=true
MAX_CACHED_STREAMS=1000

# Metrics Configuration
METRICS_ENABLED=true
PROMETHEUS_PORT=8007

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*

# Logging Configuration
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_FILE=/var/log/maximus/auditory_cortex.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5

# Optional: Advanced Features
ENABLE_ADAPTIVE_THRESHOLDS=false
ENABLE_MULTI_SCALE_FFT=false
ENABLE_DEEP_CORRELATION=false
SAVE_SPECTROGRAMS=false
SPECTROGRAM_OUTPUT_PATH=/data/spectrograms
