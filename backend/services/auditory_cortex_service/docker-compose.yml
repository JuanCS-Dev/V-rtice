version: '3.8'
services:
  auditory_cortex_service:
    build: .
    container_name: vertice-auditory-cortex-service
    hostname: vertice-auditory-cortex-service
    ports:
    - 8007:8007
    environment:
    - SERVICE_NAME=auditory_cortex_service
    - SERVICE_HOST=0.0.0.0
    - SERVICE_PORT=8005
    - LOG_LEVEL=INFO
    - ENVIRONMENT=development
    - REDIS_URL=redis://redis:6379
    - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/db
    - MONGO_URL=mongodb://mongo:27017/db
    networks:
    - maximus-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8005/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on: {}
  auditory_cortex_service-sidecar:
    image: vertice-registry-sidecar:latest
    container_name: vertice-auditory-cortex-service-sidecar
    environment:
    - SERVICE_NAME=auditory_cortex_service
    - SERVICE_HOST=vertice-auditory-cortex-service
    - SERVICE_PORT=8005
    - SERVICE_HEALTH_ENDPOINT=/health
    - REGISTRY_URL=http://vertice-register-lb:80
    - HEARTBEAT_INTERVAL=30
    - INITIAL_WAIT_TIMEOUT=60
    depends_on:
      auditory_cortex_service:
        condition: service_started
    networks:
    - maximus-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 64M
networks:
  maximus-network:
    external: true
