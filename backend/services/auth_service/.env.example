# ============================================================================
# Maximus Authentication Service - Environment Configuration
# ============================================================================
# This file documents all required and optional environment variables.
# Copy this to `.env` and fill in your actual values.
#
# SECURITY NOTICE:
# - NEVER commit the actual .env file to version control
# - .env should be in .gitignore
# - Use different values for dev/staging/production
# ============================================================================

# ----------------------------------------------------------------------------
# JWT Configuration - REQUIRED
# ----------------------------------------------------------------------------
# Secret key for signing JWT tokens
# CRITICAL: Must be at least 32 characters for security
#
# Generate a secure key with one of these commands:
#   openssl rand -hex 32
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# Example (DO NOT USE THIS VALUE):
JWT_SECRET_KEY=CHANGE_ME_MINIMUM_32_CHARACTERS_REQUIRED_USE_OPENSSL_RAND_HEX_32

# JWT algorithm (default: HS256)
# Options: HS256, HS384, HS512
JWT_ALGORITHM=HS256

# Token expiration time in minutes (default: 30)
# Recommended: 15-60 for access tokens
JWT_EXPIRATION_MINUTES=30

# ----------------------------------------------------------------------------
# Service Configuration
# ----------------------------------------------------------------------------
# Service version for metrics/tracing
SERVICE_VERSION=1.0.0

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# Database Configuration (if applicable)
# ----------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:pass@localhost:5432/auth_db

# ----------------------------------------------------------------------------
# Constitutional v3.0 Configuration
# ----------------------------------------------------------------------------
# Enable JSON-formatted logs (true/false)
# JSON_LOGS=true

# Metrics export port (if different from main service port)
# METRICS_PORT=9090

# Tracing configuration
# JAEGER_AGENT_HOST=localhost
# JAEGER_AGENT_PORT=6831

# ============================================================================
# DEPLOYMENT CHECKLIST
# ============================================================================
# Before deploying to production:
#
# 1. [ ] Generate a NEW secure JWT_SECRET_KEY
# 2. [ ] Verify key length is >= 32 characters
# 3. [ ] Set appropriate JWT_EXPIRATION_MINUTES
# 4. [ ] Configure production database credentials
# 5. [ ] Set LOG_LEVEL to INFO or WARNING
# 6. [ ] Enable monitoring/tracing endpoints
# 7. [ ] Verify .env is NOT committed to git
# 8. [ ] Use secret management system (AWS Secrets Manager, etc.)
# ============================================================================
