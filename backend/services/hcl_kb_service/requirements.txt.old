## HCL Knowledge Base Service - Requirements
# Production-ready dependencies

# FastAPI + ASGI server
fastapi>=0.115.0  # Security fixes for starlette
uvicorn[standard]>=0.32.0  # Compatibility with updated fastapi
python-multipart>=0.0.18  # CVE-2024-53981 fix

# Database (async PostgreSQL + TimescaleDB)
sqlalchemy[asyncio]>=2.0.25  # Keep current
asyncpg>=0.29.0  # Keep current
psycopg2-binary>=2.9.9  # Keep current
alembic>=1.13.1  # Keep current

# Validation
pydantic>=2.9.0  # Latest security patches
pydantic-settings>=2.1.0  # Keep current
email-validator>=2.1.0  # Keep current

# Utilities
python-dotenv>=1.0.0  # Keep current
python-jose[cryptography]>=3.4.0  # CVE-2024-33663, CVE-2024-33664 fixes
passlib[bcrypt]>=1.7.4  # Keep current

# Monitoring
prometheus-client==0.19.0

# Logging
python-json-logger==2.0.7

# Testing
pytest>=7.4.4  # Keep current
pytest-asyncio>=0.23.3  # Keep current
httpx>=0.27.0  # Security updates

# Type checking
mypy>=1.8.0  # Keep current
types-python-jose>=3.3.4.8  # Keep current
anyio>=4.4.0  # Security fix for race condition
