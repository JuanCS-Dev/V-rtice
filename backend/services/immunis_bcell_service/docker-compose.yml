version: '3.8'
services:
  immunis_bcell_service:
    build: .
    container_name: vertice-immunis-bcell-service
    hostname: vertice-immunis-bcell-service
    ports:
    - 8201:8201
    environment:
    - SERVICE_NAME=immunis_bcell_service
    - SERVICE_HOST=0.0.0.0
    - SERVICE_PORT=8201
    - LOG_LEVEL=INFO
    - ENVIRONMENT=development
    - REDIS_URL=redis://redis:6379
    - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/db
    - MONGO_URL=mongodb://mongo:27017/db
    networks:
    - maximus-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8201/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on: {}
  immunis_bcell_service-sidecar:
    image: vertice-registry-sidecar:latest
    container_name: vertice-immunis-bcell-service-sidecar
    environment:
    - SERVICE_NAME=immunis_bcell_service
    - SERVICE_HOST=vertice-immunis-bcell-service
    - SERVICE_PORT=8201
    - SERVICE_HEALTH_ENDPOINT=/health
    - REGISTRY_URL=http://vertice-register-lb:80
    - HEARTBEAT_INTERVAL=30
    - INITIAL_WAIT_TIMEOUT=60
    depends_on:
      immunis_bcell_service:
        condition: service_started
    networks:
    - maximus-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 64M
networks:
  maximus-network:
    external: true
