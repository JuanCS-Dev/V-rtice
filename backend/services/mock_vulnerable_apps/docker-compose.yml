version: '3.8'
services:
  mock_vulnerable_apps:
    build: .
    container_name: vertice-mock-vulnerable-apps
    hostname: vertice-mock-vulnerable-apps
    ports:
    - 8294:8294
    environment:
    - SERVICE_NAME=mock_vulnerable_apps_service
    - SERVICE_HOST=0.0.0.0
    - SERVICE_PORT=8294
    - LOG_LEVEL=INFO
    - ENVIRONMENT=development
    - REDIS_URL=redis://redis:6379
    - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/db
    - MONGO_URL=mongodb://mongo:27017/db
    networks:
    - maximus-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8294/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on: {}
  mock_vulnerable_apps-sidecar:
    image: vertice-registry-sidecar:latest
    container_name: vertice-mock-vulnerable-apps-sidecar
    environment:
    - SERVICE_NAME=mock_vulnerable_apps_service
    - SERVICE_HOST=vertice-mock-vulnerable-apps
    - SERVICE_PORT=8294
    - SERVICE_HEALTH_ENDPOINT=/health
    - REGISTRY_URL=http://vertice-register-lb:80
    - HEARTBEAT_INTERVAL=30
    - INITIAL_WAIT_TIMEOUT=60
    depends_on:
      mock_vulnerable_apps:
        condition: service_started
    networks:
    - maximus-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 64M
networks:
  maximus-network:
    external: true
