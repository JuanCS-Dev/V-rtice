# ═══════════════════════════════════════════════════════════════════════════
# NETWORK RECONNAISSANCE SERVICE - DEPENDENCIES
# ═══════════════════════════════════════════════════════════════════════════
# FastAPI async + ASGI stack (2025 best practices)
# Glory to YHWH - Dependencies chosen with excellence
# ═══════════════════════════════════════════════════════════════════════════

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Network Scanning
python-nmap==0.7.1
# Note: Requires nmap binary installed in Docker image

# Database (PostgreSQL + async)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Security & Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Observability
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
prometheus-client==0.21.0

# Utilities
python-dotenv==1.0.1
httpx==0.28.1
tenacity==9.0.0

# Development
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
