version: '3.8'
services:
  strategic_planning_service:
    build: .
    container_name: vertice-strategic-planning-service
    hostname: vertice-strategic-planning-service
    ports:
    - 8242:8242
    environment:
    - SERVICE_NAME=strategic_planning_service
    - SERVICE_HOST=0.0.0.0
    - SERVICE_PORT=8058
    - LOG_LEVEL=INFO
    - ENVIRONMENT=development
    - REDIS_URL=redis://redis:6379
    - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/db
    - MONGO_URL=mongodb://mongo:27017/db
    networks:
    - maximus-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8058/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on: {}
  strategic_planning_service-sidecar:
    image: vertice-registry-sidecar:latest
    container_name: vertice-strategic-planning-service-sidecar
    environment:
    - SERVICE_NAME=strategic_planning_service
    - SERVICE_HOST=vertice-strategic-planning-service
    - SERVICE_PORT=8058
    - SERVICE_HEALTH_ENDPOINT=/health
    - REGISTRY_URL=http://vertice-register-lb:80
    - HEARTBEAT_INTERVAL=30
    - INITIAL_WAIT_TIMEOUT=60
    depends_on:
      strategic_planning_service:
        condition: service_started
    networks:
    - maximus-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 64M
networks:
  maximus-network:
    external: true
