apiVersion: apps/v1
kind: Deployment
metadata:
  name: maximus-predict
  namespace: vertice
  labels:
    app: maximus-predict
    tier: consciencia
    layer: camada-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: maximus-predict
  template:
    metadata:
      labels:
        app: maximus-predict
        tier: consciencia
    spec:
      containers:
      - name: maximus-predict
        image: us-east1-docker.pkg.dev/projeto-vertice/vertice-images/maximus_predict:latest
        ports:
        - containerPort: 8154
          name: http
        env:
        - name: SERVICE_NAME
          value: "maximus-predict"
        - name: SERVICE_HOST
          value: "0.0.0.0"
        - name: SERVICE_PORT
          value: "8154"
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: vertice-global-config
              key: LOG_LEVEL
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: vertice-global-config
              key: ENVIRONMENT
        - name: POSTGRES_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/$(POSTGRES_DB)"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: vertice-core-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: vertice-core-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: vertice-core-secrets
              key: POSTGRES_DB
        - name: REDIS_URL
          value: "redis://:$(REDIS_PASSWORD)@redis:6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: vertice-core-secrets
              key: REDIS_PASSWORD
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: vertice-core-secrets
              key: GEMINI_API_KEY
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8154
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8154
          initialDelaySeconds: 20
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: maximus-predict
  namespace: vertice
  labels:
    app: maximus-predict
    tier: consciencia
spec:
  type: ClusterIP
  ports:
  - port: 8154
    targetPort: 8154
    protocol: TCP
    name: http
  selector:
    app: maximus-predict
