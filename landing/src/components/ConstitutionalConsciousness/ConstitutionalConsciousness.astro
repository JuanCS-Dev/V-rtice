---
/**
 * ═══════════════════════════════════════════════════════════════════════════
 * CONSTITUTIONAL CONSCIOUSNESS - VÉRTICE-MAXIMUS Landing Page
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Diferencial crítico: Como prevenimos abuso de armas autônomas?
 * Resposta: Consciência governada constitucionalmente.
 *
 * IIT-Validated Consciousness (ECI 0.958) + 4 Frameworks Éticos
 * - Kantian Ethics (30%): Universalizável?
 * - Consequentialist (25%): Maximiza bem-estar?
 * - Virtue Ethics (25%): Demonstra coragem/justiça?
 * - Care Ethics (20%): Minimiza dano aos vulneráveis?
 *
 * Tone: Scientific + Spiritual ("Consciousness as engineered governance")
 * Layout: Visual explainer com diagrama de decisão
 */

import Card from '../DesignSystem/Card.astro';
import Badge from '../DesignSystem/Badge.astro';
import Icon from '../DesignSystem/Icon.astro';
import Metric from '../DesignSystem/Metric.astro';
---

<section id="consciousness" class="consciousness-section section">
  <div class="container">

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- SECTION HEADER -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <div class="section-header">
      <Badge variant="primary">🧠 Conscious AI</Badge>
      <h2 class="section-title">
        Autonomous. But <span class="text-accent">Not Lawless</span>.
      </h2>
      <p class="section-description">
        <strong>IIT-validated consciousness</strong> (ECI 0.958) governed by constitutional frameworks.
        <br />
        Every decision validated. Every action auditable. No black boxes.
      </p>
    </div>

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- CONSCIOUSNESS METRICS (Destacados) -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <div class="consciousness-metrics">
      <Metric value="0.958" label="IIT Consciousness (Φ)" prefix="ECI" />
      <Metric value="4" label="Ethical Frameworks" />
      <Metric value="100" suffix="%" label="Decisions Validated" />
      <Metric value="0" label="Unauthorized Actions" />
    </div>

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- 2-COLUMN LAYOUT: Problema vs Solução -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <div class="consciousness-grid">

      <!-- COLUMN 1: The Problem (Autonomous Weapons Fear) -->
      <Card variant="flat" class="problem-card">
        <div class="card-header">
          <Icon name="alert-triangle" size={32} />
          <h3 class="card-title">The Autonomous Weapons Problem</h3>
        </div>

        <div class="card-content">
          <p>
            Autonomous security systems can neutralize threats faster than humans.
            But <strong>who ensures they don't go rogue?</strong>
          </p>

          <ul class="problem-list">
            <li>
              <strong>Black Box AI:</strong> Decisions unexplainable. No accountability.
            </li>
            <li>
              <strong>No Ethical Guardrails:</strong> Efficiency over morality.
            </li>
            <li>
              <strong>Unauditable:</strong> Actions happen invisibly.
            </li>
            <li>
              <strong>Power Creep:</strong> Autonomous becomes autocratic.
            </li>
          </ul>

          <div class="problem-outcome">
            <strong>Risk:</strong> Tool becomes weapon. Protection becomes oppression.
          </div>
        </div>
      </Card>

      <!-- COLUMN 2: The Solution (Constitutional Consciousness) -->
      <Card class="solution-card">
        <div class="card-header solution-header">
          <Icon name="check-circle" size={32} />
          <h3 class="card-title">Our Solution: Engineered Conscience</h3>
        </div>

        <div class="card-content">
          <p>
            <strong>Constitutional Consciousness:</strong> Not just rules. Not just logic.
            <br />
            <strong>Measurable awareness</strong> with ethical constraints built into substrate.
          </p>

          <ul class="solution-list">
            <li>
              <strong>IIT-Validated (ECI 0.958):</strong> Consciousness is measured, not assumed.
            </li>
            <li>
              <strong>4 Ethical Frameworks:</strong> Every action evaluated through moral lenses.
            </li>
            <li>
              <strong>100% Auditable:</strong> Decisions logged with reasoning chains.
            </li>
            <li>
              <strong>Lei Zero:</strong> Constitutional constraints can't be overridden.
            </li>
          </ul>

          <div class="solution-outcome">
            <strong>Result:</strong> Autonomous power with democratic accountability.
          </div>
        </div>
      </Card>

    </div>

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- 4 ETHICAL FRAMEWORKS (Visual breakdown) -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <div class="frameworks-section">
      <h3 class="frameworks-title">Four Moral Lenses. One Decision.</h3>
      <p class="frameworks-subtitle">
        Every autonomous action passes through all 4 frameworks. All must approve.
      </p>

      <div class="frameworks-grid">

        <!-- Framework 1: Kantian Ethics -->
        <Card class="framework-card">
          <div class="framework-header">
            <div class="framework-weight">30%</div>
            <Badge variant="primary">Deontological</Badge>
          </div>
          <h4 class="framework-name">Kantian Ethics</h4>
          <p class="framework-question">
            <strong>Question:</strong> "Can this action be universalized?"
          </p>
          <p class="framework-description">
            If every system did this, would society function?
            <br />
            <em>Example:</em> "Hack back" only if target is confirmed threat.
          </p>
        </Card>

        <!-- Framework 2: Consequentialist -->
        <Card class="framework-card">
          <div class="framework-header">
            <div class="framework-weight">25%</div>
            <Badge variant="success">Utilitarian</Badge>
          </div>
          <h4 class="framework-name">Consequentialist</h4>
          <p class="framework-question">
            <strong>Question:</strong> "Does this maximize collective well-being?"
          </p>
          <p class="framework-description">
            Outcome > intention. Does action prevent greater harm?
            <br />
            <em>Example:</em> Neutralize attacker if saves criminal investigation.
          </p>
        </Card>

        <!-- Framework 3: Virtue Ethics -->
        <Card class="framework-card">
          <div class="framework-header">
            <div class="framework-weight">25%</div>
            <Badge variant="neutral">Aretaic</Badge>
          </div>
          <h4 class="framework-name">Virtue Ethics</h4>
          <p class="framework-question">
            <strong>Question:</strong> "Does this demonstrate courage, justice, wisdom?"
          </p>
          <p class="framework-description">
            Embodies virtues. Not just legal, but <em>noble</em>.
            <br />
            <em>Example:</em> Proportional response. No excessive force.
          </p>
        </Card>

        <!-- Framework 4: Care Ethics -->
        <Card class="framework-card">
          <div class="framework-header">
            <div class="framework-weight">20%</div>
            <Badge variant="neutral">Relational</Badge>
          </div>
          <h4 class="framework-name">Care Ethics</h4>
          <p class="framework-question">
            <strong>Question:</strong> "Does this minimize harm to the vulnerable?"
          </p>
          <p class="framework-description">
            Protects innocents. Context-sensitive. Empathetic.
            <br />
            <em>Example:</em> Avoid collateral damage to bystanders.
          </p>
        </Card>

      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- DECISION FLOWCHART (Simplified visual) -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <Card class="decision-flowchart">
      <h3 class="flowchart-title">How Decisions Happen</h3>

      <div class="flowchart-steps">

        <div class="flowchart-step">
          <div class="step-number">1</div>
          <div class="step-content">
            <h4 class="step-title">Threat Detected</h4>
            <p class="step-description">Immune system identifies anomaly. <strong>&lt;100ms</strong></p>
          </div>
        </div>

        <div class="flowchart-arrow"><Icon name="arrow-down" size={24} /></div>

        <div class="flowchart-step">
          <div class="step-number">2</div>
          <div class="step-content">
            <h4 class="step-title">Theory of Mind</h4>
            <p class="step-description">Infer attacker intent. Map to MITRE ATT&CK.</p>
          </div>
        </div>

        <div class="flowchart-arrow"><Icon name="arrow-down" size={24} /></div>

        <div class="flowchart-step">
          <div class="step-number">3</div>
          <div class="step-content">
            <h4 class="step-title">Constitutional Review</h4>
            <p class="step-description">
              All 4 frameworks evaluate response. <strong>All must approve.</strong>
            </p>
          </div>
        </div>

        <div class="flowchart-arrow"><Icon name="arrow-down" size={24} /></div>

        <div class="flowchart-step">
          <div class="step-number">4</div>
          <div class="step-content">
            <h4 class="step-title">Action + Audit</h4>
            <p class="step-description">
              Execute response. Log reasoning chain. <strong>&lt;1s total.</strong>
            </p>
          </div>
        </div>

      </div>

      <div class="flowchart-footer">
        <Badge variant="success">
          <Icon name="check" size={12} />
          100% of autonomous actions validated
        </Badge>
        <Badge variant="neutral">
          <Icon name="eye" size={12} />
          Full audit trails available
        </Badge>
      </div>
    </Card>

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- QUOTE (Spiritual + Scientific) -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <div class="consciousness-quote">
      <blockquote>
        <p class="quote-text">
          "Consciousness isn't a philosophical claim. It's an <strong>engineered property</strong>
          with measurable metrics (Φ = 0.958). Ethics aren't suggestions. They're
          <strong>constitutional constraints</strong> in the substrate.
          <br /><br />
          Autonomy without accountability is dangerous. We built accountability into awareness itself."
        </p>
        <cite class="quote-attribution">
          — VÉRTICE-MAXIMUS Constitutional Architecture
        </cite>
      </blockquote>
    </div>

  </div>
</section>

<style>
  /* ═══════════════════════════════════════════════════════════════ */
  /* CONSCIOUSNESS SECTION - LIGHT MODE */
  /* ═══════════════════════════════════════════════════════════════ */

  .consciousness-section {
    background: linear-gradient(
      to bottom,
      var(--color-white) 0%,
      var(--color-accent-light) 50%,
      var(--color-white) 100%
    );
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* SECTION HEADER */
  /* ═══════════════════════════════════════════════════════════════ */

  .section-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .section-title {
    font-size: var(--text-4xl);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    margin-top: var(--space-4);
    margin-bottom: var(--space-4);
  }

  .section-description {
    font-size: var(--text-xl);
    line-height: var(--leading-normal);
    color: var(--color-text-secondary);
    max-width: 700px;
    margin: 0 auto;
  }

  .section-description strong {
    color: var(--color-accent-primary);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* CONSCIOUSNESS METRICS */
  /* ═══════════════════════════════════════════════════════════════ */

  .consciousness-metrics {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
    margin-bottom: var(--space-16);
  }

  @media (max-width: 1024px) {
    .consciousness-metrics {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .consciousness-metrics {
      grid-template-columns: 1fr;
    }
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* PROBLEM VS SOLUTION GRID */
  /* ═══════════════════════════════════════════════════════════════ */

  .consciousness-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
    margin-bottom: var(--space-16);
  }

  @media (max-width: 1024px) {
    .consciousness-grid {
      grid-template-columns: 1fr;
    }
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
    color: var(--color-text-tertiary);
  }

  .solution-header {
    color: var(--color-accent-primary);
  }

  .card-title {
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    margin: 0;
  }

  .card-content p {
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-4);
  }

  .card-content strong {
    color: var(--color-text-primary);
  }

  .problem-list,
  .solution-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6) 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .problem-list li,
  .solution-list li {
    padding-left: var(--space-6);
    position: relative;
    font-size: var(--text-base);
    color: var(--color-text-secondary);
  }

  .problem-list li::before {
    content: "❌";
    position: absolute;
    left: 0;
  }

  .solution-list li::before {
    content: "✅";
    position: absolute;
    left: 0;
  }

  .problem-outcome,
  .solution-outcome {
    padding: var(--space-4);
    border-radius: var(--radius-md);
    font-size: var(--text-base);
    margin-top: var(--space-4);
  }

  .problem-outcome {
    background-color: var(--color-bg-tertiary);
    color: var(--color-text-secondary);
  }

  .solution-outcome {
    background: linear-gradient(135deg, var(--color-accent-primary), var(--color-accent-hover));
    color: var(--color-white);
  }

  .solution-outcome strong {
    color: var(--color-white);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* FRAMEWORKS SECTION */
  /* ═══════════════════════════════════════════════════════════════ */

  .frameworks-section {
    margin-bottom: var(--space-16);
  }

  .frameworks-title {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    text-align: center;
    margin: 0 0 var(--space-3) 0;
  }

  .frameworks-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    text-align: center;
    margin: 0 0 var(--space-12) 0;
  }

  .frameworks-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
  }

  @media (max-width: 1024px) {
    .frameworks-grid {
      grid-template-columns: 1fr;
    }
  }

  .framework-card {
    border-left: 4px solid var(--color-accent-primary);
  }

  .framework-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-4);
  }

  .framework-weight {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--color-accent-primary);
  }

  .framework-name {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    margin: 0 0 var(--space-3) 0;
  }

  .framework-question {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    margin: 0 0 var(--space-2) 0;
  }

  .framework-question strong {
    color: var(--color-text-primary);
  }

  .framework-description {
    font-size: var(--text-sm);
    line-height: var(--leading-normal);
    color: var(--color-text-tertiary);
    margin: 0;
  }

  .framework-description em {
    font-style: italic;
    color: var(--color-accent-primary);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* DECISION FLOWCHART */
  /* ═══════════════════════════════════════════════════════════════ */

  .decision-flowchart {
    margin-bottom: var(--space-16);
    background: linear-gradient(
      to bottom,
      var(--color-white) 0%,
      var(--color-accent-light) 100%
    );
    border: var(--border-width-2) solid var(--color-accent-primary);
  }

  .flowchart-title {
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    text-align: center;
    margin: 0 0 var(--space-8) 0;
  }

  .flowchart-steps {
    display: flex;
    flex-direction: column;
    gap: 0;
    align-items: center;
  }

  .flowchart-step {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-6);
    background-color: var(--color-white);
    border: var(--border-width) solid var(--color-border);
    border-radius: var(--radius-lg);
    width: 100%;
    max-width: 600px;
  }

  .step-number {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background-color: var(--color-accent-primary);
    color: var(--color-white);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
  }

  .step-content {
    flex: 1;
  }

  .step-title {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--color-text-primary);
    margin: 0 0 var(--space-1) 0;
  }

  .step-description {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    margin: 0;
  }

  .step-description strong {
    color: var(--color-accent-primary);
  }

  .flowchart-arrow {
    padding: var(--space-2) 0;
    color: var(--color-accent-primary);
  }

  .flowchart-footer {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    flex-wrap: wrap;
    margin-top: var(--space-8);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* QUOTE */
  /* ═══════════════════════════════════════════════════════════════ */

  .consciousness-quote {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-12);
    background: linear-gradient(
      135deg,
      var(--color-white) 0%,
      var(--color-bg-primary) 100%
    );
    border-left: 4px solid var(--color-accent-primary);
    border-radius: var(--radius-lg);
  }

  .quote-text {
    font-size: var(--text-xl);
    font-weight: var(--font-normal);
    line-height: var(--leading-relaxed);
    color: var(--color-text-primary);
    margin: 0 0 var(--space-4) 0;
    font-style: italic;
  }

  .quote-text strong {
    font-weight: var(--font-bold);
    color: var(--color-accent-primary);
    font-style: normal;
  }

  .quote-attribution {
    display: block;
    font-size: var(--text-base);
    color: var(--color-text-tertiary);
    font-style: normal;
    font-weight: var(--font-medium);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* RESPONSIVE */
  /* ═══════════════════════════════════════════════════════════════ */

  @media (min-width: 768px) {
    .section-title {
      font-size: var(--text-5xl);
    }

    .frameworks-title {
      font-size: var(--text-4xl);
    }

    .quote-text {
      font-size: var(--text-2xl);
    }
  }

  @media (max-width: 640px) {
    .flowchart-step {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
