---
/**
 * ═══════════════════════════════════════════════════════════════════════════
 * HERO SECTION CLEAN - VÉRTICE-MAXIMUS Landing Page
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Hero refatorado para tema CLARO (inspirado em Claude.ai)
 * Filosofia: Hospital Cibernético - Clean, Professional, Trustworthy
 *
 * Storytelling B2G:
 * - Headline impactante mas objetivo (não agressivo)
 * - Foco em proteção e inteligência (não ataque)
 * - CTAs claros e específicos
 * - Trust signals com SSP-GO
 *
 * Performance: Lighthouse 95+ target
 * Accessibility: WCAG 2.1 AA compliant
 */

import Button from '../DesignSystem/Button.astro';
import Badge from '../DesignSystem/Badge.astro';
import Icon from '../DesignSystem/Icon.astro';
---

<section class="hero-clean section-lg">
  <div class="container">
    <div class="hero-content">

      <!-- ═══════════════════════════════════════════════════════════ -->
      <!-- ANNOUNCEMENT BADGE (Launch) -->
      <!-- ═══════════════════════════════════════════════════════════ -->
      <div class="hero-badge fade-in-up">
        <Badge variant="primary">
          🧬 Cybersecurity Biomimética para Segurança Pública
        </Badge>
      </div>

      <!-- ═══════════════════════════════════════════════════════════ -->
      <!-- HERO HEADLINE (8 palavras max, objetivo) -->
      <!-- ═══════════════════════════════════════════════════════════ -->
      <h1 class="hero-title fade-in-up delay-100">
        Defesa Cibernética Biomimética
        <br />
        <span class="hero-title-accent">para Segurança Pública</span>
      </h1>

      <!-- ═══════════════════════════════════════════════════════════ -->
      <!-- HERO SUBTITLE (valor mensurável, 1 linha) -->
      <!-- ═══════════════════════════════════════════════════════════ -->
      <p class="hero-subtitle fade-in-up delay-200">
        Plataforma inteligente que aprende, adapta e protege
        <strong>operações críticas de investigação criminal</strong>
      </p>

      <!-- ═══════════════════════════════════════════════════════════ -->
      <!-- HERO DESCRIPTION (contexto adicional, objetivo) -->
      <!-- ═══════════════════════════════════════════════════════════ -->
      <p class="hero-description fade-in-up delay-300">
        VÉRTICE-MAXIMUS não é apenas software. É um <strong>organismo cibernético</strong>
        que opera como sistema imunológico digital: detecta ameaças, aprende padrões,
        e fortalece defesas automaticamente.
        <br /><br />
        Arquitetura biomimética com 103+ microserviços autônomos garante disponibilidade
        contínua mesmo sob ataque. <strong>Quando adversários entram, suas ações são
        rastreadas, neutralizadas e documentadas para inteligência criminal.</strong>
      </p>

      <!-- ═══════════════════════════════════════════════════════════ -->
      <!-- CTA BUTTONS (claros, específicos, não genéricos) -->
      <!-- ═══════════════════════════════════════════════════════════ -->
      <div class="hero-ctas fade-in-up delay-400">
        <Button variant="primary" size="lg" href="#contato">
          Agendar Demonstração
          <Icon name="arrow-right" size={20} />
        </Button>

        <Button variant="secondary" size="lg" href="#arquitetura">
          Ver Arquitetura
          <Icon name="layers" size={20} />
        </Button>
      </div>

      <!-- ═══════════════════════════════════════════════════════════ -->
      <!-- TRUST SIGNALS (SSP-GO + métricas) -->
      <!-- ═══════════════════════════════════════════════════════════ -->
      <div class="hero-trust-signals fade-in-up delay-500">
        <div class="trust-badge-group">
          <Badge variant="success">
            <Icon name="check" size={12} />
            Cliente: SSP-GO
          </Badge>
          <Badge variant="neutral">
            Em Produção desde 2024
          </Badge>
          <Badge variant="neutral">
            103+ Microserviços
          </Badge>
        </div>

        <div class="trust-stats">
          <div class="trust-stat">
            <div class="trust-stat-value">9</div>
            <div class="trust-stat-label">Arquiteturas Biomimeticas</div>
          </div>
          <div class="trust-stat">
            <div class="trust-stat-value">24/7</div>
            <div class="trust-stat-label">Disponibilidade</div>
          </div>
          <div class="trust-stat">
            <div class="trust-stat-value">100%</div>
            <div class="trust-stat-label">Código Aberto</div>
          </div>
        </div>
      </div>

    </div>

    <!-- ═══════════════════════════════════════════════════════════ -->
    <!-- HERO VISUAL (screenshot/diagrama clean) -->
    <!-- ═══════════════════════════════════════════════════════════ -->
    <div class="hero-visual fade-in-up delay-600">
      <div class="hero-visual-container">
        <!-- Placeholder: substituir por screenshot real do dashboard ou diagrama -->
        <div class="hero-visual-placeholder">
          <svg viewBox="0 0 1200 800" fill="none" xmlns="http://www.w3.org/2000/svg">
            <!-- Dashboard mockup placeholder -->
            <rect width="1200" height="800" fill="var(--color-bg-secondary)" rx="12"/>

            <!-- Header bar -->
            <rect x="0" y="0" width="1200" height="60" fill="var(--color-white)" />
            <circle cx="30" cy="30" r="8" fill="var(--color-accent-primary)" opacity="0.3"/>
            <rect x="50" y="20" width="150" height="20" fill="var(--color-text-tertiary)" opacity="0.2" rx="4"/>

            <!-- Sidebar -->
            <rect x="0" y="60" width="250" height="740" fill="var(--color-white)" opacity="0.5"/>

            <!-- Main content area with cards -->
            <rect x="280" y="90" width="350" height="200" fill="var(--color-white)" rx="8" opacity="0.8"/>
            <rect x="660" y="90" width="510" height="200" fill="var(--color-white)" rx="8" opacity="0.8"/>

            <rect x="280" y="320" width="510" height="250" fill="var(--color-white)" rx="8" opacity="0.8"/>
            <rect x="820" y="320" width="350" height="250" fill="var(--color-white)" rx="8" opacity="0.8"/>

            <!-- Accent elements -->
            <circle cx="450" cy="150" r="40" fill="var(--color-accent-primary)" opacity="0.1"/>
            <circle cx="900" cy="160" r="60" fill="var(--color-accent-primary)" opacity="0.08"/>
            <circle cx="550" cy="450" r="80" fill="var(--color-accent-primary)" opacity="0.06"/>

            <!-- Text placeholder lines -->
            <rect x="300" y="110" width="200" height="12" fill="var(--color-text-tertiary)" opacity="0.2" rx="2"/>
            <rect x="300" y="135" width="150" height="8" fill="var(--color-text-tertiary)" opacity="0.15" rx="2"/>

            <rect x="680" y="110" width="180" height="12" fill="var(--color-text-tertiary)" opacity="0.2" rx="2"/>
            <rect x="680" y="135" width="220" height="8" fill="var(--color-text-tertiary)" opacity="0.15" rx="2"/>

            <!-- Chart elements -->
            <polyline points="300,510 370,480 440,500 510,460 580,470 650,440 720,450"
                      stroke="var(--color-accent-primary)" stroke-width="3" fill="none" opacity="0.6"/>
          </svg>
        </div>

        <div class="hero-visual-label">
          <Icon name="shield" size={16} />
          <span>Dashboard de Investigação Criminal</span>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
  /* ═══════════════════════════════════════════════════════════════ */
  /* HERO CLEAN STYLES */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-clean {
    position: relative;
    background: linear-gradient(
      to bottom,
      var(--color-white) 0%,
      var(--color-bg-primary) 100%
    );
    overflow: hidden;
  }

  /* Opcional: Background pattern sutil */
  .hero-clean::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: radial-gradient(
      circle at 1px 1px,
      var(--color-border) 1px,
      transparent 0
    );
    background-size: 40px 40px;
    opacity: 0.3;
    pointer-events: none;
  }

  .hero-clean .container {
    position: relative;
    z-index: 1;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    padding-bottom: var(--space-16);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* BADGE */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-badge {
    margin-bottom: var(--space-6);
    opacity: 0;
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* TITLE */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-title {
    font-size: var(--text-4xl);
    font-weight: var(--font-bold);
    line-height: var(--leading-tight);
    color: var(--color-text-primary);
    margin-bottom: var(--space-6);
    letter-spacing: var(--tracking-tight);
    opacity: 0;
  }

  .hero-title-accent {
    color: var(--color-accent-primary);
    display: inline-block;
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* SUBTITLE */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-subtitle {
    font-size: var(--text-xl);
    line-height: var(--leading-normal);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
    opacity: 0;
  }

  .hero-subtitle strong {
    color: var(--color-text-primary);
    font-weight: var(--font-semibold);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* DESCRIPTION */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-description {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-10);
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    opacity: 0;
  }

  .hero-description strong {
    color: var(--color-text-primary);
    font-weight: var(--font-medium);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* CTAs */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-ctas {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: var(--space-12);
    opacity: 0;
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* TRUST SIGNALS */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-trust-signals {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    align-items: center;
    opacity: 0;
  }

  .trust-badge-group {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
    justify-content: center;
  }

  .trust-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
    width: 100%;
    max-width: 600px;
  }

  .trust-stat {
    text-align: center;
  }

  .trust-stat-value {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--color-accent-primary);
    line-height: var(--leading-none);
    margin-bottom: var(--space-2);
  }

  .trust-stat-label {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    font-weight: var(--font-medium);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* HERO VISUAL */
  /* ═══════════════════════════════════════════════════════════════ */

  .hero-visual {
    margin-top: var(--space-16);
    opacity: 0;
  }

  .hero-visual-container {
    position: relative;
    border-radius: var(--radius-2xl);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    border: var(--border-width) solid var(--color-border);
    background: var(--color-white);
  }

  .hero-visual-placeholder {
    width: 100%;
    height: auto;
    display: block;
  }

  .hero-visual-placeholder svg {
    width: 100%;
    height: auto;
    display: block;
  }

  .hero-visual-label {
    position: absolute;
    bottom: var(--space-6);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--color-backdrop);
    backdrop-filter: blur(8px);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    font-weight: var(--font-medium);
    border: var(--border-width) solid var(--color-border);
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* RESPONSIVE (Mobile-first) */
  /* ═══════════════════════════════════════════════════════════════ */

  @media (min-width: 768px) {
    .hero-title {
      font-size: var(--text-5xl);
    }

    .hero-subtitle {
      font-size: var(--text-2xl);
    }

    .hero-description {
      font-size: var(--text-lg);
    }
  }

  @media (min-width: 1024px) {
    .hero-title {
      font-size: var(--text-6xl);
    }

    .hero-ctas {
      gap: var(--space-6);
    }
  }

  @media (max-width: 640px) {
    .trust-stats {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .hero-ctas {
      flex-direction: column;
      width: 100%;
    }

    .hero-ctas :global(.btn) {
      width: 100%;
    }
  }

  /* ═══════════════════════════════════════════════════════════════ */
  /* ANIMATIONS (fade-in-up já definido no design-system) */
  /* ═══════════════════════════════════════════════════════════════ */

  /* As classes .fade-in-up e .delay-* já estão no design-system-clean.css */
  /* Aqui apenas garantimos que sejam aplicadas */
</style>

<script>
  /**
   * Intersection Observer para animar elementos no scroll
   * (Progressive Enhancement)
   */
  if ('IntersectionObserver' in window) {
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    // Observa todos os elementos com classe fade-in-up
    document.querySelectorAll('.hero-clean .fade-in-up').forEach(el => {
      observer.observe(el);
    });
  } else {
    // Fallback: mostrar tudo imediatamente
    document.querySelectorAll('.hero-clean .fade-in-up').forEach(el => {
      (el as HTMLElement).style.opacity = '1';
    });
  }
</script>
