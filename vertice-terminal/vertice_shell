#!/usr/bin/env python3
"""
VÉRTICE - Direct Shell Launcher
Launches the interactive shell immediately.
"""
import sys
import os

# Add the vertice-terminal directory to the path
sys.path.insert(0, os.path.join(os.path.dirname(__file__)))

if __name__ == "__main__":
    from vertice.interactive_shell import main
    main()
